import AppRoutes from "./config/AppRoutes";

const App = () => {
  return <AppRoutes />;
};

export default App;

/// Aplicatia va folosii urmatoarele librarii:

// styled-components
// redux toolkit
// react router

// Aplicatia va fi full responsive (mobile, desktop)
