import styled from "styled-components";
import { FONTS } from "../../constants/variables";

export const ButtonSideMenu = styled.button`
  font-family: ${FONTS.poppins}, sans-serif;
  border: none;
  background: none;
`;
